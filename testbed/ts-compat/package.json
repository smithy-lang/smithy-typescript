{
  "private": true,
  "name": "@smithy/ts-compat-test",
  "description": "Verifies that generated clients compile against every supported TypeScript minor version.",
  "type": "module",
  "scripts": {
    "test": "node ./run.mjs"
  }
}
